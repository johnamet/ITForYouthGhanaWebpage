import { AdminPlaceholder } from "@/components/admin/admin-placeholder";

export default function AdminNewTestimonialPage() {
  return (
    <AdminPlaceholder
      title="Create Testimonial"
      description="The create route is scaffolded for future testimonial entry."
      nextSteps={["Add profile fields, quotes, media, and initiative assignment."]}
    />
  );
}
